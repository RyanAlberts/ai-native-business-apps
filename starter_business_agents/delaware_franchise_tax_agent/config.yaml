# Delaware Franchise Tax Calculator — provider config
provider: claude
model: claude-sonnet-4-6
auth_mode: subscription
temperature: 0.1   # Tax procedure — keep variation minimal.
max_tokens: 4096

mcp_servers: {}
allowed_tools: []

extra:
  max_turns: 4
